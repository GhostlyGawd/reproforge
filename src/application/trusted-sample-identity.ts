/**
 * Stable public principal for the bundled, non-customer-code fixture.
 *
 * Keep the existing value so cases created by earlier REST releases remain
 * readable from every keyless surface, including MCP.
 */
export const TRUSTED_SAMPLE_CALLER_ID = "rest:anonymous-trusted-sample";
